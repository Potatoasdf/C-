#ifndef NODE_H
#define NODE_H
#include <iostream>
#include <cstring>

using namespace std;

class Node {
 public:
  Node(int newNum);
  ~Node();
  Node* getNext();
  int getNum();
  void setNext(Node* newNext);
 private:
  int num;
  Node* next;
};
#endif
