#ifndef TREENODE_H
#define TREENODE_H
#include <iostream>

using namespace std;

class treenode
{
 public:
  treenode(char input);
  ~treenode();
  treenode* getR();
  void setR(treenode* newR);
  treenode* getL();
  void setL(treenode* newL);
  char getVar();
 private:
  char var;
  treenode* right;
  treenode* left;
};

#endif
